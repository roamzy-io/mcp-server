---
name: Bug report
about: Report a problem with @roamzy/mcp-server
title: '[Bug] '
labels: bug
---

## What happened

<!-- Describe the unexpected behaviour in one or two sentences. -->

## Reproducer

**MCP client:** <!-- e.g. Claude Desktop 0.x, Cursor 0.x, custom client -->
**Server version:** <!-- `npm view @roamzy/mcp-server version`, or check your install -->
**Tool / method called:** <!-- e.g. `roamzy_create_order`, `initialize`, `tools/list` -->
**Arguments (sanitised — DO NOT paste real tokens):**

```json
{ }
```

**Response you got:**

```json
{ }
```

**Response you expected:**

```text

```

## Environment

- Operating system:
- Node.js version (`node --version`):
- Install method: <!-- `npx -y @roamzy/mcp-server` / hosted tarball / built from source -->
- Anonymous mode or pre-existing token: <!-- ROAMZY_API_TOKEN set? -->

## Anything else

<!-- Logs, screenshots, additional context. If logs include any token-looking
     strings (rk_live_*, eyJ*, MSISDN, payment hashes), please redact them. -->
